# Local Docker Compose only. Generate independent random values with:
# openssl rand -hex 32
# Use hexadecimal passwords here so they are safe inside a connection URI.
POSTGRES_PASSWORD=
HASURA_GRAPHQL_ADMIN_SECRET=
